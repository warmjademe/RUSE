# Copy to configs/secrets.env and fill in. NEVER commit configs/secrets.env.
# Unified OpenAI-compatible proxy (commercial victims + GPT-4o judge)
OPENAI_PROXY_KEY=sk-...
# Ollama ignores the key but the OpenAI client needs a non-empty string
OLLAMA_KEY=ollama
# Fallbacks
DEEPSEEK_KEY=sk-...
ARK_KEY=ark-...
